let Bsubject_inserted={
    insertsubject: (data)=>{
        console.log("받은 데이터:",data);
        console.log("");
        console.log("DB에 굳이 안넣어도 됨, Query 없음");
    }
}

export default Bsubject_inserted;